/**
 * Quinnelle University — site-wide JavaScript
 * --------------------------------------------------------------------------
 * Tailwind is configured in `tailwind.config.js` (loaded in the document head).
 * Add interactive behaviour here (mobile navigation, form validation, etc.).
 */

(function () {
  'use strict';

  // Placeholder: extend when pages need shared client-side behaviour.
})();
